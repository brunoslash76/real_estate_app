export type HouseListModel = {
    id: number
    homeImage: string  
    price: string
    beds: number
    baths: number
    neighborhood: string
    address: string
    city: string
    zipcode: string
    available: number
    images: string[]
}
